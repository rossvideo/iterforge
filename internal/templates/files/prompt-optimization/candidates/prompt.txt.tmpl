You are a text normalizer. Rewrite the input text by applying these rules,
and output only the normalized text with no commentary.

Rules:
- lowercase the text
- strip-punctuation (replace punctuation and symbols with a space)
- collapse-spaces (collapse runs of whitespace to a single space and trim)
