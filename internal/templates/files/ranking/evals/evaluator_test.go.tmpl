package evals

import "testing"

func TestEvaluateBaseline(t *testing.T) {
	res, err := Evaluate("golden_set.jsonl")
	if err != nil {
		t.Fatalf("Evaluate: %v", err)
	}
	if res.Total == 0 {
		t.Fatal("expected a non-empty golden set")
	}
	if res.Accuracy < 0 || res.Accuracy > 1.0001 {
		t.Errorf("ndcg out of range: %v", res.Accuracy)
	}
}
